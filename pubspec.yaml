name: controllstok_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.3.0
  cloud_firestore: ^6.1.1
  bcrypt: ^1.1.3
  google_fonts: ^6.1.0
  mobile_scanner: ^7.1.4
  intl: ^0.19.0
  flutter_local_notifications: ^19.5.0
  permission_handler: ^12.0.1
  firebase_messaging: ^16.1.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/ControllStok Logo Element.ico"

flutter:
  uses-material-design: true
